---

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: production
helmCharts:
  - releaseName: backend
    name: helm-chart
    repo: oci://ghcr.io/xfoucron/charts
    namespace: production
    valuesFile: common-values.yaml
    additionalValuesFiles:
      - values.yaml
