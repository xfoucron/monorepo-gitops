---

envSpecific:
  - name: APP_DOMAIN
    value: "http://prod.notes.local"

resources:
  requests:
    cpu: 512m
    memory: 512Mi
  limits:
    cpu: 512m
    memory: 512Mi

ingress:
  hosts:
    - host: prod.notes-api.local
      paths:
        - path: /
          pathType: Prefix
